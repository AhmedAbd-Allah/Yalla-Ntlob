<%= form_with(model: order_item, local: true) do |form| %>
  <% if order_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_item.errors.count, "error") %> prohibited this order_item from being saved:</h2>

      <ul>
      <% order_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :order_item_user_id %>
  </div>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id, id: :order_item_order_id %>
  </div>

  <div class="field">
    <%= form.label :item %>
    <%= form.text_field :item, id: :order_item_item %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount, id: :order_item_amount %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :order_item_price %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_field :comment, id: :order_item_comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
