<%= form_with(model: order_invitation, local: true) do |form| %>
  <% if order_invitation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_invitation.errors.count, "error") %> prohibited this order_invitation from being saved:</h2>

      <ul>
      <% order_invitation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :order_invitation_user_id %>
  </div>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id, id: :order_invitation_order_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :order_invitation_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
